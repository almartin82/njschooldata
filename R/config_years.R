# Compatibility note ----------------------------------------------------------
#
# Year constants and validation coverage moved to R/source_registry.R. Keeping
# this file avoids surprising tools or downstream contributors that still look
# for the historical configuration filename; it intentionally defines no
# independent values or functions.
