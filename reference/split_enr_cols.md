# Split enrollment columns

Splits enrollment columns that combine IDs and names (pre 2009-10
format)

## Usage

``` r
split_enr_cols(df)
```

## Arguments

- df:

  An enrollment data frame (eg output of \`get_raw_enr\`)

## Value

Data frame with split columns
