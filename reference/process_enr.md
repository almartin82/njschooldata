# Process a NJ enrollment file

Does cleanup of dataframes returned by \`get_raw_enr\`.

## Usage

``` r
process_enr(df)
```

## Arguments

- df:

  An enrollment data frame (eg output of \`get_raw_enr\`)

## Value

Cleaned and processed enrollment data frame
