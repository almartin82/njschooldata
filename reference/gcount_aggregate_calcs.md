# Aggregate multiple grad count rows and produce summary statistics

Aggregate multiple grad count rows and produce summary statistics

## Usage

``` r
gcount_aggregate_calcs(df)
```

## Arguments

- df:

  grouped df of gcount data

## Value

df with aggregate stats for whatever grouping was provided
