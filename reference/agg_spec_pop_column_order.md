# Helper function to return aggregate special populations columns in correct order

Helper function to return aggregate special populations columns in
correct order

## Usage

``` r
agg_spec_pop_column_order(df)
```

## Arguments

- df:

  aggregate special populations df

## Value

data_frame
