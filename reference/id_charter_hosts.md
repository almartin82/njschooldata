# Identify charter host districts

Identify charter host districts

## Usage

``` r
id_charter_hosts(df)
```

## Arguments

- df:

  dataframe of NJ school data containing \`district_id\` column

## Value

df with host district id and name for every matching record
