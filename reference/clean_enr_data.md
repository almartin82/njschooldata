# Clean enrollment data types

All columns come back as character; coerce some back to numeric.

## Usage

``` r
clean_enr_data(df)
```

## Arguments

- df:

  An enrollment data frame with standardized column names

## Value

Data frame with correct data types
