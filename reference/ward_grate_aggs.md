# Aggregates grad rate data by ward

Aggregates grad rate data by ward

## Usage

``` r
ward_grate_aggs(df)
```

## Arguments

- df:

  output of `fetch_grad_rate`

## Value

A data frame of ward aggregations
