# Calculate DFG peer percentile by grade

calculates DFG percentile by grade/test

## Usage

``` r
dfg_peer_percentile(df)
```

## Arguments

- df:

  data.frame with PARCC/assessment data containing DFG and required
  columns

## Value

data.frame with percent proficient and scale score percentile rank
