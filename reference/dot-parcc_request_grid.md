# Build the registered PARCC/NJSLA request grid

Build the registered PARCC/NJSLA request grid

## Usage

``` r
.parcc_request_grid(include_science = TRUE)
```

## Arguments

- include_science:

  Include science assessments (2019+)? Default is TRUE.

## Value

A data frame with one row per requested year, grade, and subject.
