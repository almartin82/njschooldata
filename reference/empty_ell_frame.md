# Empty tidy EL frame (correctly typed)

Returned for an unavailable year and used to validate the schema.

## Usage

``` r
empty_ell_frame()
```

## Value

zero-row data.frame with \[ELL_TIDY_COLS\]
