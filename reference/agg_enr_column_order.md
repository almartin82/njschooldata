# Helper function to return aggregate enrollment columns in correct order

Helper function to return aggregate enrollment columns in correct order

## Usage

``` r
agg_enr_column_order(df)
```

## Arguments

- df:

  aggregate enrollment dataframe

## Value

data.frame
