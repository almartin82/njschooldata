# Clean SPR subgroup names

Standardizes subgroup names from SPR database to match package naming
conventions.

## Usage

``` r
clean_spr_subgroups(group)
```

## Arguments

- group:

  Vector of subgroup names

## Value

Vector of cleaned subgroup names
