# Internal helper to map a column name

Internal helper to map a column name

## Usage

``` r
clean_name(df_names, clean_list)
```

## Arguments

- df_names:

  Column name to clean

- clean_list:

  Named list mapping old names to new names

## Value

Cleaned column name
