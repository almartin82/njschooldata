# Tidy up the grade level field on enrollment data

Tidy up the grade level field on enrollment data

## Usage

``` r
clean_enr_grade(df)
```

## Arguments

- df:

  An enrollment data file. clean_enr_grade is part of a set of chained
  cleaning functions that live inside process_enr.

## Value

Data frame with cleaner grade_level column
