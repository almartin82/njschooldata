# Helper function to return aggregate sped columns in correct order

Helper function to return aggregate sped columns in correct order

## Usage

``` r
agg_sped_column_order(df)
```

## Arguments

- df:

  aggregate sped dataframe

## Value

data.frame
