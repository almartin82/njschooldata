# Identify graduation aggregation levels

Adds boolean flags to identify state, county, district, and school level
records.

## Usage

``` r
id_grad_aggs(df)
```

## Arguments

- df:

  Graduation dataframe, output of tidy_grad_rate or tidy_grad_count

## Value

data.frame with boolean aggregation flags
