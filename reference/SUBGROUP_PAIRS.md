# Standard subgroup pairs for gap analysis

Pre-defined pairs of subgroups commonly used for achievement gap
calculations. Each pair consists of c(reference_group, comparison_group)
where gap = reference - comparison.

## Usage

``` r
SUBGROUP_PAIRS
```

## Format

An object of class `list` of length 6.
