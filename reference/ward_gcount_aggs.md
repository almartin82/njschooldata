# Aggregates grad counts data by ward

Aggregates grad counts data by ward

## Usage

``` r
ward_gcount_aggs(df)
```

## Arguments

- df:

  output of `fetch_grad_count`

## Value

A data frame of ward aggregations
