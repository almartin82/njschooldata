# Grad Count column order

Puts graduation count data frame columns in standard order.

## Usage

``` r
gcount_column_order(df)
```

## Arguments

- df:

  Processed grad count df

## Value

Data frame with columns in correct order
