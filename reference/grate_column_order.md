# Grad Rate column order

Puts graduation rate data frame columns in standard order.

## Usage

``` r
grate_column_order(df)
```

## Arguments

- df:

  Processed grad rate df

## Value

Data frame with columns in correct order
