# Extract data from a source result

Extract data from a source result

## Usage

``` r
source_result_data(result, allow_partial = FALSE)
```

## Arguments

- result:

  A source result.

- allow_partial:

  If \`TRUE\`, return the result's data even when its source status is
  not \`actual\`. The default is strict.

## Value

The parsed data.
