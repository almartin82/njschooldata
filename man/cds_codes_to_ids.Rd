% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_cleaning.R
\name{cds_codes_to_ids}
\alias{cds_codes_to_ids}
\title{Rename CDS \code{*_code} columns to \code{*_id}}
\usage{
cds_codes_to_ids(df)
}
\arguments{
\item{df}{data frame that may contain \code{county_code}, \code{district_code},
and/or \code{school_code} columns}
}
\value{
df, with any present \code{*_code} CDS columns renamed to \code{*_id}
}
\description{
Converts the \code{county_code}, \code{district_code}, and \code{school_code}
columns produced by \code{\link{clean_cds_fields}} to the package-standard
\code{*_id} names. \code{clean_cds_fields} stays \code{*_code} because it is
the shared raw-name normalizer; call this immediately after it on any data
family whose public output should use the \code{*_id} convention.
}
\keyword{internal}
