% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_absence.R
\name{fetch_absence_multi}
\alias{fetch_absence_multi}
\title{Fetch Chronic Absenteeism Data for Multiple Years}
\usage{
fetch_absence_multi(
  end_years,
  level = "school",
  type = "chronic",
  tidy = TRUE,
  use_cache = TRUE
)
}
\arguments{
\item{end_years}{Integer vector of school years (e.g., \code{2017:2024}).}

\item{level}{One of \code{"school"} or \code{"district"}.}

\item{type}{One of \code{"chronic"}, \code{"by_grade"}, \code{"days_absent"},
or \code{"essa"}.}

\item{tidy}{Logical; if \code{TRUE} (default), normalizes subgroup names.}

\item{use_cache}{Logical; if \code{TRUE} (default), caches each year.}
}
\value{
A data frame with all years bound together.
}
\description{
Convenience wrapper that calls \code{fetch_absence()} for each year and
binds the results. Skips years that fail (e.g., COVID gap 2020-2021) with
a warning.
}
\examples{
\dontrun{
# Get all available years of chronic absenteeism
ca_all <- fetch_absence_multi(2017:2024)

# COVID gap: 2020-2021 were not reported, will be skipped with a warning
ca_all <- fetch_absence_multi(2017:2024)

# Multi-year trend by district
library(dplyr)
ca_all \%>\%
  filter(subgroup == "total", is_state) \%>\%
  select(end_year, chronically_absent_rate)
}
}
