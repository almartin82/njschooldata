% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source_transport.R
\name{download_source}
\alias{download_source}
\title{Download and validate a registered NJ DOE source}
\usage{
download_source(
  url,
  source_type,
  cache_path = NULL,
  timeout = 60,
  retries = 2L,
  allowed_hosts = source_host_allowlist(),
  allow_http = FALSE,
  request_fn = .default_source_request,
  sleep_fn = Sys.sleep
)
}
\arguments{
\item{url}{HTTPS source URL.}

\item{source_type}{One of `xlsx`, `xls`, `zip`, `csv`, `text`, `json`, or
`html`.}

\item{cache_path}{Optional validated artifact cache path.}

\item{timeout}{Bounded request timeout in seconds.}

\item{retries}{Number of retries after the initial transient failure.}

\item{allowed_hosts}{Explicit host allowlist, defaulting to registered hosts.}

\item{allow_http}{Permit plaintext HTTP for a narrowly scoped historical
source. Active sources should leave this `FALSE`.}

\item{request_fn}{Injectable request implementation for offline tests.}

\item{sleep_fn}{Injectable retry delay implementation.}
}
\value{
An `njsd_source_result` whose data is the validated local path.
}
\description{
Downloads are written to a temporary file, validated, and then atomically
promoted into an optional cache path. Transport failures and artifact/parser
failures have distinct source statuses.
}
\keyword{internal}
