name: Source validation release gate

on:
  release:
    types: [published]
  workflow_dispatch:

permissions: read-all

jobs:
  affected-release-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Verify lock and current evidence
        run: python3 tools/source-validation/release_gate.py
